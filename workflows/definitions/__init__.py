# -*- coding: utf-8 -*-
"""
Workflow Definitions Sub-package.

This directory is intended to store example or pre-defined workflow
definition files in YAML or JSON format.

Workflow definition files describe the sequence of nodes, their configurations,
and how they are connected to form a data processing pipeline.
"""

# This __init__.py file makes Python treat the directory as a package.
# It can be empty or contain package-level initializations if needed.

print("workflows.definitions package initialized") # Placeholder

